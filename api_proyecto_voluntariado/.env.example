# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
# IMPORTANT: Generate a secure random string for production
# Use: php bin/console secrets:generate-keys
# Or online: https://www.random.org/strings/?num=1&len=32&digits=on&upperalpha=on&loweralpha=on&unique=on&format=plain
APP_SECRET=CHANGE_ME_TO_RANDOM_32_CHARACTERS
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> DATABASE CONFIGURATION ###
# For local development (XAMPP/MySQL):
# DATABASE_URL="mysql://root:@127.0.0.1:3306/PROYECTOINTER?serverVersion=10.4.32-MariaDB&charset=utf8mb4"

# For Docker or SQL Server:
# DATABASE_URL="sqlsrv://sa:YOUR_PASSWORD_HERE@127.0.0.1:1433/PROYECTOINTER?serverVersion=2019&Encrypt=no"

# Default (Docker-ready):
DATABASE_URL="sqlsrv://sa:CHANGE_DB_PASSWORD@db:1433/PROYECTOINTER?"
###< DATABASE CONFIGURATION ###

###> symfony/mailer ###
# For development, disable mailer or use a test service
MAILER_DSN=null://null
# For Gmail (not recommended for production):
# MAILER_DSN=gmail://username:password@default
###< symfony/mailer ###

###> symfony/google-mailer ###
# Gmail SHOULD NOT be used on production, use it in development only.
# MAILER_DSN=gmail://USERNAME:PASSWORD@default
###< symfony/google-mailer ###

###> FIREBASE CONFIGURATION ###
# Get your Firebase API Key from: Firebase Console > Project Settings > General
FIREBASE_API_KEY=YOUR_FIREBASE_WEB_API_KEY_HERE

# Firebase Service Account Credentials Path
# Place your firebase_service_account.json in config/secrets/
# Symfony's kreait_firebase.yaml adds %kernel.project_dir%/ automatically
GOOGLE_APPLICATION_CREDENTIALS=config/secrets/firebase_service_account.json
###< FIREBASE CONFIGURATION ###

###> nelmio/cors-bundle ###
# CORS: Orígenes permitidos (regex)
# Desarrollo: permite localhost y 127.0.0.1 con cualquier puerto
# Producción: cambia a tu dominio específico, ej: '^https://tudominio\.com$'
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
###< nelmio/cors-bundle ###
